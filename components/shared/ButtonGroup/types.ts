export type ButtonItem = {
    id?: string,
    name: string,
    subHeader?: string,
    socialList?: string[],
    link?: string,
    imgLink?: string,
    default?: boolean,
    active?: boolean,
    cssClass?: string
};